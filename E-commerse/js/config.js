/* =========================================================
   SITE CONFIG
   API_BASE = address of your backend (the Render service).
   On localhost the site automatically talks to http://localhost:3000
========================================================= */
window.SHOPEASE_CONFIG = {
    API_BASE: "https://lester1301-github-io.onrender.com"
};
